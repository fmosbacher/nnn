# NNN - Not a proper Neural Net

Neural network and matrix library. Supports only sigmoid activation function and training with the entire dataset, no batching.

## Usage

There is a binary crate with an example.

```bash
cargo run
```
